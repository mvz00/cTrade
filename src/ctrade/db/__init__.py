"""Database layer: engine, ORM models, repositories, mappers, persistence.

Imports from sub-modules are kept lazy to avoid circular dependencies.
Use direct imports like ``from ctrade.db.engine import init_db`` or
``from ctrade.db.persistence import fire_and_forget``.
"""
